{% extends 'base.html.twig' %}

{% block body %}

    <div class="alert alert-success">{{ message }}</div>
    <h1>Add new user</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
