{% block body %}
    <h1>User list</h1>
    <ul>
        {% for user in users %}
            <li>
                {{ user.name }} ({{ user.email }})
                <a href="#"">Edit</a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}